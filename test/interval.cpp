#include "catch.hpp"
#include "src/interval.h"

SCENARIO( "ggl::interval is correctly constructed", "[interval]" ) {
    GIVEN( "" ) {
        WHEN( "" ) {
            THEN( "" ) {
                REQUIRE( 1 != 1 );
            }
        }
    }
}
