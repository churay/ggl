#include <cmath>
#include <memory>
#include <sstream>
#include <utility>

namespace ggl {

int xform::test() {
    return 42;
}

/*
template <class... Ts>
matrix<Ts, sizeof...(Ts), sizeof...(Ts)> xform::scale( Ts&&... pValues ) {
    return matrix<>();
}
*/

// ggl::xform::scale( 1.0f, 2.0f );

}
